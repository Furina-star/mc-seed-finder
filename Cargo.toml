[package]
name = "mc-seed-finder"
version = "0.1.0"
edition = "2021"

[dependencies]
rayon = "1.10"
clap = { version = "4", features = ["derive"] }

[build-dependencies]
cc = "1"

[profile.release]
lto = true
codegen-units = 1
